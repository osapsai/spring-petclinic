# database init, supports mysql
#database=mysql
spring.datasource.url=jdbc:mysql://db.osapsai.com:3306/prod
spring.datasource.username=dbuser
spring.datasource.password=Qwerty123
#spring.datasource.url=jdbc:mysql://localhost:3306/schema?verifyServerCertificate=false&useSSL=false&requireSSL=false


server.port=8080

# Web
spring.thymeleaf.mode=HTML

# JPA
spring.jpa.hibernate.ddl-auto=none

logging.file=application.log

# Logging
logging.level.org.springframework=INFO
#logging.level.org.springframework.web=DEBUG
# logging.level.org.springframework.context.annotation=TRACE

# Internationalization
spring.messages.basename=messages/messages

# Actuator / Management
management.endpoints.web.base-path=/manage

# Spring Boot 1.5 makes actuator secure by default
management.endpoints.web.enabled=true

# Active Spring profiles
spring.profiles.active=production
